# Optional frontend-only settings.
# This override is used by resumable uploads. Other API calls use the
# same-origin /api/v1 path through the local nginx/Vite proxy.
VITE_API_BASE_URL=/api/v1

# Leave empty to disable Yandex Metrika in local development.
VITE_YM_ID=
